# css变量

```js
 - declare ： --[name]
 - use : var(name,[defautl])
 - grammar : 
   1. classify
      - rules :  the variable only be property name,not for value
      - string:  the variable able to concat with string
      - number:  
   2. scoped 
      - the same with scss
```